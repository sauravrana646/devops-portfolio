/** Empty until real credentials are added. Pages show an honest empty state. */
export const featuredCerts: readonly {
  tag: string;
  title: string;
  body: string;
  verifyUrl: string;
}[] = [];

export const certList: readonly {
  title: string;
  issuer: string;
  issued: string;
  verifyUrl: string;
}[] = [];
